repositories:
  - id: sample
    label: Sample repository
    root: /absolute/path/to/your/repo
    defaultPath: README.md
    # Local Reader App reads local state only and does not fetch from Git remotes.
    excludes:
      - .git
      - node_modules
      - dist
